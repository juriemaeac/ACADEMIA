import 'package:flutter/material.dart';


class Todolist extends StatelessWidget {
  
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('To-do List'),
      ),
      body: Center(child: Text('hatdog'),
      ),
    );
  }
}
